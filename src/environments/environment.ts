export const environment = {
  production: false,
  apiBase: ''
};




// ng serve --host 0.0.0.0 --ssl true --ssl-cert ./cert.pem --ssl-key ./key.pem --proxy-config proxy.conf.json
// # OR your ./start-dev.sh (which uses auto-detected IP)
// ./start-dev.sh


//my final code

// ng serve --host 0.0.0.0 --ssl true --ssl-cert ./cert.pem --ssl-key ./key.pem --proxy-config proxy.conf.json